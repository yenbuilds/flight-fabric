import '../../../shared/aircraft-presets.js';

export const { groups: presetGroups, numberValue: presetNumberValue, brightnessLabel } = (typeof globalThis !== 'undefined' ? globalThis : window).FlightFabricPresets;
